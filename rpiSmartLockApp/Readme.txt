Smart Lock Application:

Makefile usage:
 make <cmd> 
 cmd: 
     clean  -Removes all the files contained in /output directory
     all    -Builds the application and all the object files required.
             The objects built are selected based on the parameters set in Makefile's 'Hardware system configuration' section. 
 